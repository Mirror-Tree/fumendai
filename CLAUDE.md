# 楽譜表示Webアプリ（FP-30X連携）

Roland FP-30X（電子ピアノ）と連携し、Androidタブレット/スマホのブラウザで
「弾いた音の五線譜リアルタイム表示」を行う自作Webアプリ。
将来的にPDF楽譜表示を同一画面に統合する構想がある。

## 利用者について

- PC・IT関係に詳しい（応用情報技術者試験レベル）。技術的な説明は省略せず、根拠を示すこと
- 回答は結論を先に書く。比較表を歓迎する
- 推測は「推測」と明示する。メリットだけでなくデメリット・リスクも必ず挙げる
- 誤りがあればはっきり指摘する
- 開発PCはWindows環境

## ファイル構成

| ファイル | 役割 |
|---|---|
| `index.html` | 本番アプリ（五線譜リアルタイム表示＋PDF楽譜表示）。単一HTMLファイル。実行時にGoogle Fonts・PDF.jsをCDNから読み込む（外部ライブラリを同梱はしない） |
| `check.html` | MIDI接続の検証ページ。受信ログの採取に使う。本番の切り分け用に残す |

GitHub Pagesで公開し、Android実機のブラウザから `https://<user>.github.io/<repo>/` で開く。

## 実機で検証済みの事実（推測ではない）

2026-09-13、`check.html` で確認：

- **接続経路：Bluetooth MIDI → MIDI BLE Connect（補助アプリ）→ ブラウザのWeb MIDI が動作する**
  - OS側で接続した機器が、ブラウザのWeb MIDIから見えることを確認済み
- 端末：Galaxy Z Fold6（SM-F956Q）/ Android 16
- ブラウザ：Sleipnir 3.8.5（Chromium 152ベースのWebView）。**WebViewでもWeb MIDIは動作した**
- **Web Bluetooth APIは使用不可**（WebViewのため）。ブラウザから直接BLE接続する方式は採れない
- 安定性：2分41秒で予期しない切断0回、693メッセージ受信、取りこぼしの兆候なし
- 音域：C8まで正常受信。黒鍵（C#5, D#5, F#5, A#4）正常
- MIDIチャンネルは常に ch1
- **ダンパーペダル（CC64）は連続値で届く**。1回の踏み込みで5〜7メッセージ、0〜127の中間値を含む
  → 64以上/未満の閾値で判定し、状態変化時のみ処理すること
- **Active Sensing等の定期信号はほぼ送られてこない**（受信数の内訳計算から）

## 未検証の事項

- USBケーブル接続（USB-C to USB-B）。Bluetoothで足りているため未実施
- 10分以上の長時間使用での安定性
- 押鍵から画面表示までの実測遅延
- Unicodeのト音記号 `U+1D11E` / ヘ音記号 `U+1D122` がこの端末のフォントにあるか
  （`index.html` は未対応時にそれぞれG・F入りの丸印へフォールバックする実装になっている）
- Screen Wake Lock API が Sleipnir で動くか
- PDF.js（CDN経由）がSleipnir/WebViewで正常に動作するか。Workerの生成やcanvas描画がWebView環境で制限を受けないか未確認
- PDFのタッチ操作（1本指パン・2本指ピンチ）が実機で意図通り動くか。PCで疑似ポインターイベントによりロジックは確認済みだが、
  実際のタッチ入力・Sleipnirのジェスチャー処理との干渉は未確認
- 拡大時の canvas 実画素上限 `PDF_MAX_PIXELS=10e6` が実機のメモリ・描画速度に対して妥当か

## index.html の仕様（利用者が選択したもの）

| 項目 | 仕様 |
|---|---|
| 音符の表示 | 右端から出て左へ流れる。既定10秒で画面を横断（4〜24秒で可変） |
| 異名同音 | ♯表記を濃く、♭表記を薄く並べて併記。音名も「F♯5 / G♭5」 |
| 譜表 | **大譜表（ト音＋ヘ音）**。中央C(MIDI60)以上をト音、未満をヘ音に振り分け |
| 音の長さ | 押している間、音符から右へバーが伸びる |
| ペダル | 画面下に踏み込み状態を表示。五線譜上には記号を書かない |
| 鍵盤 | 88鍵の帯で押鍵位置を表示（非表示可） |
| 設定の保存 | 音名表示・速さ・異名同音併記・音の長さ表示・鍵盤表示・画面ロック・表示モード・分割比率を`localStorage`に保存し次回起動時に復元。MIDI入力選択とPDFは対象外 |
| 表示モード | 上部タブで「五線譜のみ／PDFのみ／分割」を切り替え（7.6インチ機での画面の狭さに対応） |
| 分割比率 | 分割表示時、境界バーをドラッグして五線譜側の高さを15〜85%で変更（キーボードの↑↓でも5%刻み） |

### 譜表位置の実装

MIDIノート番号 → ダイアトニック段位 `(floor(n/12)-1)*7 + 音名字インデックス` で計算（半段単位）。
基準：中央ド C4=28、ト音側 E4=30（下端線）〜F5=38（上端線）、ヘ音側 G2=18（下端線）〜A3=26（上端線）。
中央Cはヘ音上端線・ト音下端線のどちらからも2（＝加線1本）の等距離で、ト音側に加線1本で表示する。

各譜表ごとに `MARGIN=11`（半段単位）を超えて離れた音はオクターブ単位でその譜表に寄せ、
`8va` / `8vb` / `15mb` 等の記号を赤で添える。全88鍵が両譜表の範囲内に収まることを
ロジック単体テストで確認済み（A0=8vb/ヘ音、C8=8va/ト音、C4=ト音・加線1本）。

### PDF楽譜表示

PDF.js（CDN）で描画。「PDFを開く」から `<input type=file>` で端末内のPDFを選び、
`ArrayBuffer`のままメモリ上で扱う（サーバー送信・保存はしない。著作権保護のため毎回選び直す設計）。

- **縦横比は常に維持**。canvasの表示サイズ(CSS)＝ページサイズ×倍率とし、表示領域より大きい部分はスクロールで見る
  （以前は汎用の`canvas{width:100%;height:100%}`がPDF側にも効いて引き伸ばされていた不具合を修正）
- 開いた時は「幅合わせ」。拡大・縮小ボタンは表示領域の中心を基準に×1.25／÷1.25（倍率0.25〜6）
- 操作：タッチは1本指でパン、2本指でピンチ（指の中点の楽譜位置を固定して拡大縮小）。PCはドラッグでパン、Ctrl＋ホイールで拡大縮小
  - `touch-action:none` でブラウザ標準のパン/ピンチを止めてJSで処理している。そのため**慣性スクロールは無い**
- 拡大縮小中は表示サイズだけ即時変更（一時的にぼやける）、操作が180ms止まったら高解像度で再描画。
  再描画は裏のcanvasに描いてから差し替える（描画中に白く消えないように）。実画素は `PDF_MAX_PIXELS` で上限を設ける

## 制約・方針

- **単一HTMLファイルを維持する**（GitHub Pagesに置くだけで動く状態を保つ）。ただしGoogle Fonts・PDF.jsのようにCDN経由で実行時に読み込む追加リソースは許容する（ファイル自体は1つ、というのが実質的な意味）
- **楽譜PDFをリポジトリに入れない**。購入楽譜の著作権のため、PDFは端末内から都度読み込む設計にする（実装済み。ページ位置等も保存しない）
- 設定値（表示系オプション・表示モード）は`localStorage`で永続化済み。MIDI入力選択とPDFは対象外（理由は上記仕様表を参照）
- 低スペック端末でも動くよう、描画はノートが存在する間だけ requestAnimationFrame を回す

## 今後の予定

1. `index.html` の実機での使用感を確認（流れる速さ、異名同音併記の見やすさ、8vb表示の実用性、大譜表の見やすさ）
2. 長時間使用でのBluetooth安定性の確認
3. PDF.jsがSleipnir/WebViewで動くかの実機確認（未検証事項を参照）。分割表示モードでの操作性確認
   - 現在の主力端末は7.6インチのため、分割表示は使い勝手が厳しい可能性がある→タブ切り替えでPDF/五線譜をそれぞれ全画面表示できる設計にして緩和済み
   - 大画面Androidタブレットの追加購入を検討中（SoCはGeekbench 6シングルコア700点以上が目安）
